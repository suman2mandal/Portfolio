import React from 'react';
import Switcher from "@/components/Nav/Mode/Switcher";

const Navbar = () => {
    return (
        <div>
           <Switcher/>
        </div>
    );
};

export default Navbar;